package javaleads.lesson2;

public interface Product {
  /*Create an interface called “product”. This interface should have following A method named addStock
 * that returns total stock statistics . The method accepts two parameters productname and quantity*/
	
	public void addStock(String productName, int quantity); 
	 
}
